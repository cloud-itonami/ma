{:paths ["src"]
 ;; langgraph-clj brings langchain-clj transitively (both zero-/single-dep,
 ;; all .cljc -> portable to JVM / ClojureScript / nbb). It lives in the
 ;; kotoba-lang org of this workspace. This repo is standalone (forkable
 ;; outside the workspace); the :local/root paths only resolve inside the
 ;; monorepo checkout -- a standalone fork should override them with git
 ;; coordinates.
 :deps {
        ;; kotoba.lang.text, not clojure.string. Pinned as an explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, so without a floor here
        ;; this repo silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
       io.github.com-junkawasaki/langgraph-clj
        {:local/root "../../kotoba-lang/langgraph"}}
 :aliases
 {:dev {:override-deps {io.github.kotoba-lang/langchain
                        {:local/root "../../kotoba-lang/langchain"}}}
  ;; clojure -M:dev:run  -> drive the demo through one OperationActor
  :run {:main-opts ["-m" "matching.sim"]}
  ;; SECONDARY compat gate. The PRIMARY gate is nbb (see test/README.md and
  ;; the runtime-priority rule in the superproject CLAUDE.md: kotoba wasm >
  ;; clojurewasm > ClojureScript/nbb > JVM). The suite is portable .cljc and
  ;; runs unchanged on both.
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd6605cb6c0efc275e1df1736f6e90d4d73"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; static analysis -- clj-kondo from deps (no binary), errors fail CI.
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
